# 安全发布规范

> 在把文章推送到公开仓库前，请逐条自查，避免泄露敏感信息。

## 发布前脱敏检查清单

- [ ] 文章里不含任何 **API Key / Token / 密码 / 密钥**（如 FOFA、Shodan、GitHub、云厂商 key）
- [ ] 不含真实 **个人手机号、邮箱、身份证、家庭住址**
- [ ] 不含 **内网 IP、真实服务器地址、数据库连接串**
- [ ] 目标相关素材已脱敏：域名、UI 截图打码
- [ ] 引用的命令里没有 `curl -H "Authorization: ..."` 之类的凭证
- [ ] 不公开 **无厂商授权的漏洞细节 / 通用 Exploit**，按《网络安全法》与 CNVD/CVE 披露规范处理

## 推荐做法

1. **写文章时用占位符**：`YOUR_API_KEY`、`example.com`、`192.168.x.x`
2. **提交前用 grep 自查**：

   ```bash
   git grep -nE "(key|token|password|secret|api_key)" || echo "未发现敏感词"
   ```

3. **首次 push 前检查** `git status` 确认没有误加 `.env`、`config.ini` 等文件
4. **配置 GitHub 仓库为公开前**，再扫一遍历史提交（`git log -p | grep -i key`）

## 法律合规

- 仅分享**已授权**的安全研究、公开漏洞分析与学习笔记
- 不提供、不制作、不传播可用于未授权入侵的工具或脚本
- 引用他人内容注明出处，尊重版权

> 发现泄露时：立即删除文件 → `git filter-repo` 或重建仓库历史 → 轮换泄露的密钥。